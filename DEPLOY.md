# 🚀 Инструкция по отправке на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Введите название репозитория (например: `tic-tac-toe-beauty`)
3. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
4. Нажмите "Create repository"

## Шаг 2: Добавьте remote и отправьте код

После создания репозитория выполните команды (замените `YOUR_USERNAME` и `REPO_NAME` на ваши данные):

```bash
# Добавить remote (замените URL на ваш)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Отправить код
git branch -M main
git push -u origin main
```

## Альтернативный способ (через SSH):

Если у вас настроен SSH ключ:

```bash
git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

---

**Готово!** Ваш код будет на GitHub 🎉

