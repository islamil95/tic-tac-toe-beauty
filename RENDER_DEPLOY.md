# 🚀 Деплой на Render

## Настройка на Render

### Шаг 1: Создайте Web Service на Render

1. Зайдите на https://render.com
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий `tic-tac-toe-beauty`

### Шаг 2: Настройки сборки

- **Build Command:** `npm install && npm run build`
- **Start Command:** `npm start`
- **Environment:** `Node`

### Шаг 3: Переменные окружения

Добавьте в разделе "Environment Variables":

- `NODE_ENV` = `production`
- `TELEGRAM_BOT_TOKEN` = (ваш токен)
- `TELEGRAM_CHAT_ID` = (ваш chat ID)

**Порт (`PORT`) Render установит автоматически** - не добавляйте его вручную!

### Шаг 4: Деплой

Нажмите "Create Web Service" и дождитесь сборки. После деплоя ваш сайт будет доступен по адресу вида:
`https://tic-tac-toe-beauty.onrender.com`

---

## Как это работает

1. Render запускает `npm install && npm run build` - устанавливает зависимости и собирает React в папку `dist`
2. Затем запускает `npm start` (который выполняет `node server.js`)
3. `server.js` отдает статические файлы из `dist/` для фронтенда
4. API запросы на `/api/*` обрабатываются сервером
5. Все остальные запросы отдаются `index.html` (для React Router, если будет добавлен)

---

## Локальное тестирование перед деплоем

Чтобы проверить, что всё работает локально:

```bash
# 1. Соберите проект
npm run build

# 2. Запустите только сервер (он будет отдавать собранные файлы)
npm start

# 3. Откройте http://localhost:5001
```

---

**Готово!** 🎉

